# Django REST Product API

A secure backend API built using Django REST Framework with JWT authentication.

## 🚀 Features
- JWT Authentication (Login & Protected APIs)
- User-specific data access
- CRUD APIs for Products
- Token-based security
- Deployed on Render

## 🛠 Tech Stack
- Python
- Django
- Django REST Framework
- SimpleJWT
- SQLite
- Gunicorn
- Render

## 🔐 Authentication
This API uses JWT authentication.
Include the token in headers:

Authorization: Bearer <your_access_token>

## 📦 API Endpoints
- POST /api/register/
- POST /api/login/
- GET /api/products/ (protected)
- POST /api/products/ (protected)

## 🌍 Live API
Deployed on Render (backend-only project).

## 🎯 Purpose
This project demonstrates backend skills:
authentication, authorization, REST APIs, and cloud deployment.

## 👤 Author
Pavan
